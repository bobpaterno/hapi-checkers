# Checkers with jQuery
- Run `npm install`
- Run `npm install gulp -g` if, when you type `gulp` from a command line, nothing happens
- Run `gulp html`
- Run `node server.js`
- Browse to localhost:3000
